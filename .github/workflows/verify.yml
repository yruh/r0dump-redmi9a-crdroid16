name: verify

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  release-tree:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify source-only release tree
        run: scripts/verify-release-tree.sh

